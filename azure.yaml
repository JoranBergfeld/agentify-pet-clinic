name: petclinic-ai-prototype
metadata:
  template: petclinic-ai-prototype@0.0.1
services:
  web:
    project: .
    language: java
    host: appservice
    dist: target
    hooks:
      prepackage:
        shell: sh
        run: ./mvnw -q -DskipTests package
infra:
  provider: bicep
  path: infra
